spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/forezp/SpringcloudConfig/
          searchPaths: respo
          username:
          password:
      label: master
  application:
      name: ocean-config

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8888/eureka/
